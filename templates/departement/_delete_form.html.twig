<form method="post" action="{{ path('app_departement_delete', {'id': departement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" class="my-delete-form">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ departement.id) }}">
    <button type="submit" class="btn btn-danger">Delete</button>
</form>
